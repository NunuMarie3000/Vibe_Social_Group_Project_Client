//Login.js
import React, { useState } from "react";
import "../App.css";

const Login = ({login}) => {



  return (
    <div>
      <button onClick = {login}>Login</button>
    </div>
  );
};

export default Login;